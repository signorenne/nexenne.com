/**
 * The one rectangle type the brand layouts share, so a banner safe area, a card
 * safe area, and a motto highlight all describe themselves the same way.
 */
export interface Box {
	x: number;
	y: number;
	width: number;
	height: number;
}
